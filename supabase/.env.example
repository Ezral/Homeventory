# Supabase CLI / local stack
# Copy to supabase/.env for local `supabase start` Google Auth.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Used by scripts/link-and-push.sh (never commit real tokens)
# SUPABASE_ACCESS_TOKEN=
